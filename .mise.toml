[tools]
pnpm = "11.3.0"
node = "24.16.0"
actionlint = "1.7.12"
zizmor = "1.25.2"
gitleaks = "8.30.1"
lefthook = "2.1.9"

[tasks.setup]
description = "Initial project setup"
run = ["mise install", "lefthook install", "pnpm install --frozen-lockfile"]
